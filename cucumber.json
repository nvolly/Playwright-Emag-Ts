{
  "default": {
    "paths": ["tests/features/"],

    "require": ["tests/step-definition/*.ts"],

    "formatOptions": {
      "snippetInterface": "async-await"
    },

    "format": [
      ["html", "cucumber-report.html"],

      "summary",

      "progress-bar",

      "json:./cucumber-report.json"
    ]
  }
}
